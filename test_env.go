package main

import (
	"fmt"
)

func not_main() {
	app_user := "postgres"
	app_pw := "postgres"
	app_name := "postgres"

	fmt.Printf("User: %v, password: %v, name: %v", app_user, app_pw, app_name)
}
